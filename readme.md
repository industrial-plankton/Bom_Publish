# Inkcape Bom Publisher

Extention that:

- PDFs the svg
- asks for a .csv BOM
- PDFs the BOM into a table
- Appends the BOM PDF to the drawing PDF

## Install

copy files into $env:APPDATA\inkscape\extensions\
Restart Inkscape if open

## Usage

Extenstions -> Export -> Publish PDF with BOM
